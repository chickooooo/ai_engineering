# Copy to .env. Compose reads this file; never commit the real one.
# The backend's own provider keys live in backend/.env.

POSTGRES_USER=ai_engineering
POSTGRES_PASSWORD=change-me
POSTGRES_DB=ai_engineering
POSTGRES_PORT=5432
